import { colors } from 'websight-admin/theme'

import { PACKAGE_MANAGER_ROOT_PATH } from '../../../../utils/PackageManagerConstants.js';

const NavigationItemFragment = {
    title: 'Package Manager',
    img: 'dvr',
    color: colors.primaryBlue,
    description: 'Manage FileVault content packages using robust tool. Track activities using persistent logs and queues.',
    href: PACKAGE_MANAGER_ROOT_PATH
}

export default NavigationItemFragment;